# build image 
# For Nginx + Gunicorn + Uvicorn
docker build -t fastapi-nginx .

# start container
docker run -d -p 8000:80 --name fastapi-test fastapi-nginx

# run multiple containers on different host ports
docker run -d -p 80:80 --name container_1 fastapi-nginx
docker run -d -p 8080:80 --name container_2 fastapi-nginx
docker run -d -p 18880:80 --name container_3 fastapi-nginx

# 3 check running containers
docker ps

# stop container
docker stop fastapi-test

# remove container
docker rm fastapi-test

# check container log
docker logs fastapi-test

# try interactive mode:

docker run -it fastapi-nginx /bin/bash

# Then manually test:

nginx -t
gunicorn n2t.app:app -k uvicorn.workers.UvicornWorker --bind 127.0.0.1:8000

